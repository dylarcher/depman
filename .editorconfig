# We do not want to include unnecessary project files; however, yet
# at the very least, we need a baseline formatting configuration.

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false